---
import type { WritingEntry as WritingContentEntry } from "../lib/content";
import WritingStream from "./WritingStream.astro";

interface Props {
  items: WritingContentEntry[];
  projectTitles?: Record<string, string>;
}

const { items, projectTitles = {} } = Astro.props;
---
<section class="latest-writing page" id="latest-writing" aria-labelledby="latest-writing-title">
  <div class="latest-writing__head">
    <div>
      <p class="eyebrow">Latest writing</p>
      <h2 id="latest-writing-title">Notes, experiments, and ongoing work.</h2>
    </div>
  </div>

  <WritingStream
    items={items}
    projectTitles={projectTitles}
    continuationHref="/timeline/"
    continuationLabel="View all in timeline"
    markLatest={true}
    emptyLabel="No writing published yet"
  />
</section>
